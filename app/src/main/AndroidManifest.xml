<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nich.work.aequorea" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".Aequorea"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme.Light" >
        <activity
            android:name=".main.ui.activities.MainActivity"
            android:theme="@style/AppTheme.Light.Main"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.ui.activities.ArticleActivity"
            android:theme="@style/AppTheme.Light.Main"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".author.ui.AuthorActivity"
            android:theme="@style/AppTheme.Light.Main"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>